// if (!import.meta.env.VITE_APP_URL)
//   throw new Error("the environment variable VITE_APP_URL is not defined");

const baseUrl = "https://ton-swap-indexer.broxus.com/v1/"; // import.meta.env.VITE_APP_URL;

export { baseUrl };
